# v0.7.5

## Highlights
- Reset the terminal’s kitty keyboard state on exit so downstream apps (e.g., tig, Emacs) stay in legacy mode after running kc; no more lingering kitty protocol toggles.

## Testing
- `go test ./internal/ui/...`
- `go test ./cmd/kc` (no tests)
